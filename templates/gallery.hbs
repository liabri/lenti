<!DOCTYPE html>
<html lang="en-US">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link href="index.css" rel="stylesheet">
    <link href="carousel.css" rel="stylesheet">
    <title>Gallery</title>
  </head>
  <body>
    <main>

      <div id="header">
        <h1 id="title">Gallery</h1>
        <a href="collections.html" id="navigation">Collections</a>
      </div>

      <div class="container">
        <ul class="gallery">
          {{#each collections}}
          {{#each images}}
            <li class="carousel">
                <a id="{{@index}}" href="#{{@index}}">
                    <img class="carousel_thumb" src="{{thumbnail}}"/>
                    <span class="carousel_full">
                      <img src="data/albums/{{../url}}/{{file_name}}"/>
                    </span>
                </a>
                <a class="carousel_close" href="#collection"></a>
                <div class="carousel_panel"> 
                  <a class="carousel_prev" href="#{{dec @index}}">prev</a>
                  <label class="carousel_divider">/</label>
                  <a class="carousel_next" href="#{{inc @index}}">next</a>
                </div>
            </li>
          {{/each}}
          {{/each}}
        </ul>
      </div>

    </main>

    <footer>
        <div id="rights">
            All rights reserved. Contact: <a href="mailto:photos@example.com">photos@example.com</a>
      </div>
    </footer>
  </body>
</html>